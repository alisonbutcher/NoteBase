{
  "name": "notebase",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "docs:dev": "pnpm --filter docs start",
    "docs:build": "pnpm --filter docs build"
  },
  "engines": {
    "node": ">=24.0.0",
    "pnpm": ">=9.0.0"
  }
}
